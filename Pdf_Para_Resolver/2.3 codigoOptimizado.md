# 2.3 — Optimización de costes

## Configuración centralizada (config.py)

```python
MAX_TOKENS_CLASIFICADOR = 100    # Solo categoría + prioridad
MAX_TOKENS_RESPUESTA = 200       # 2-3 frases máximo
MAX_TOKENS_ANALISIS_IMAGEN = 300
TEMPERATURE_CLASIFICADOR = 0.0   # Máxima consistencia
TEMPERATURE_RESPUESTA = 0.3      # Un poco de variedad
MODELO_CLASIFICADOR = "gpt-4o-mini"
MODELO_BUSQUEDA = "text-embedding-3-small"
```

## Rate limiting

```python
import time
from functools import wraps

def rate_limit(calls_per_second=2):
    def decorator(func):
        last_called = [0]
        @wraps(func)
        def wrapper(*args, **kwargs):
            elapsed = time.time() - last_called[0]
            if elapsed < 1.0 / calls_per_second:
                time.sleep(1.0 / calls_per_second - elapsed)
            return func(*args, **kwargs)
        last_called[0] = time.time()
        return wrapper
    return decorator

@rate_limit(calls_per_second=2)
def clasificar(ticket_texto):
    # ...
```

## Coste antes y después

| Concepto | Sin optimizar | Con optimización | Ahorro |
|----------|---------------|-------------------|--------|
| Clasificación (500/día) | $0.25/día | $0.15/día | 40% |
| Respuestas (500/día) | $0.20/día | $0.05/día | 75% |
| Búsqueda semántica | $0.10/día | $0.01/día | 90% |
| Análisis imágenes (100/día) | $0.50/día | $0.25/día | 50% |
| **TOTAL** | **$1.05/día ≈ $32/mes** | **$0.46/día ≈ $14/mes** | **~56%** |